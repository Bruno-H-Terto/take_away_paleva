<h3><%= t('forms.update') %> <%= @dish.name %></h3>

<%= form_with(model: @dish, class: 'col-4', url: take_away_store_dish_path(@take_away_store, @dish)) do |form| %>
  <%= render 'form', dish: @dish, take_away_store: @take_away_store, form: form %>
<% end %>
